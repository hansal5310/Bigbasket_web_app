import 'package:flutter/material.dart';

class OffersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Offers & Coupons')),
      body: Center(child: Text('Available offers will be listed here')),
    );
  }
}
